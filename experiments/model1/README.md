# Model 1 Experiments

Reserved for Model 1 training runs, checkpoints, and run reports. Model checkpoints
and large generated artifacts are gitignored (see repository root `.gitignore`) —
this directory tracks run configuration/reports only, not binary model weights.
