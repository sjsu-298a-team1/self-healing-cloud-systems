# Model 1 Split Manifests

Reserved for Model 1's train/val/test case manifests (deterministic, seeded split
over the BARO Online Boutique case folders). To be migrated here from local work in
a follow-up issue/PR. Does not contain the raw BARO dataset itself (see repository
root `.gitignore`).
